'use client'

type Loader = {
    seconds?: number | 2;
    label?: string;
}

export default function Loader({seconds, label}: Loader){

    return (
        <div className="">

        </div>
    )
}